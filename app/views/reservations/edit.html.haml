%h2 What restaurants are you interested in?
%h3 We'll use this information to help pair you with other travelling diners with similar tastes.
%h3 (we suggest choosing 3-5)
.container
  = simple_form_for @reservation, :defaults => { :wrapper_html => {:class => 'form-group'}} do |f|
    = f.association :restaurants, |
      collection: @restaurants, |
      as: :check_boxes, |
      :label => false, |
      item_wrapper_class: 'checkbox-custom-class col-md-4 col-sm-6 ', |
      class: 'checkbox-inline', |
      :label_method => lambda { |r| reservation_labels(r).html_safe } |
    %br
    %br
    %p.col-md-12.col-sm-12
    .col-sm-8.col-sm-offset-2.col-md-6.col-md-offset-3
      = f.button :submit, :class => "btn primary"



