.col-sm-4.col-sm-offset-4
  %h1 All Reservations
  %strong= link_to 'Messages', messages_path(:mailbox=>:inbox)
  %hr

- @reservations.each do |reservation|
  - if reservation.inactived_at.nil?
    = render partial: 'reservation_partial', :locals => {:r => reservation }
    %br
    .col-sm-4.col-sm-offset-4
      %hr

//= render partial: @reservations
