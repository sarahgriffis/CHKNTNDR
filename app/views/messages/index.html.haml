.col-sm-12.col-md-6.col-md-offset-3
  %h1 Messages
  - @messages.each do |m|
    %strong= "From #{User.find(m.sender_id).full_name} to #{User.find(m.receiver_id).full_name}."
    %br
    %strong= "Regard your dining experience in #{Reservation.find(m.reservation_id).city}."
    %p= m.subject
    %p= m.body
    %hr

